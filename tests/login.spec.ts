// This test file was moved to `tests/auth/login.spec.ts` as part of project reorganization.
// Kept here as a placeholder — you can remove this file if you want to fully delete the old copy.

import { test } from "@playwright/test";

test("placeholder - login.spec moved", async () => {
  test.skip();
});
